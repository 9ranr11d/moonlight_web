import { AppDispatch } from "@redux/store";

export const getSchedulesAction = () => async (dispatch: AppDispatch) => {
  try {
  } catch (err) {
    console.error("actions/calendarAction > getSchedulesAction() :", err);
  }
};
